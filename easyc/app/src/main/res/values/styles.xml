<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/gray</item>
        <item name="colorAccent">@color/darkorange</item>
    </style>


    <!-- base set of styles that apply to all versions -->
    <style name="C_theme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/gray</item>
        <item name="colorAccent">@color/black</item>

    </style>

    <style name="text" parent="@style/C_theme">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/darkred</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@color/trans</item>
    </style>

    <style name="C_theme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="C_theme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
